# Set these before running any CDK commands.
# These are resolved automatically by CDK when using named profiles,
# but can be set explicitly for CI/CD pipelines.
CDK_DEFAULT_ACCOUNT=
CDK_DEFAULT_REGION=
